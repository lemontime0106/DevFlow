# DevFlow TODO 리스트

작성일: 2026-04-11

기준 문서:

- `docs/work-log-2026-04-11.md`

## 목적

2026-04-11 기준 구현 상태를 바탕으로, 다음 작업 우선순위와 실행 단위를 정리한다.

## 우선순위 TODO

### 1. Settings 확장

- [ ] 기본 집중 시간 설정 필드 추가
- [ ] 기본 휴식 시간 설정 필드 추가
- [ ] 타이머 시작 화면 기본값과 settings 연결
- [ ] 목표 저장과 기본 preset 저장을 구분할지 결정
- [ ] 알림 on/off 구조 초안 추가

완료 기준:

- 사용자가 settings에서 기본 집중/휴식 시간을 저장할 수 있다.
- `/timer` 진입 시 저장된 기본값이 시작 폼에 반영된다.

### 2. 카테고리 관리 시작

- [ ] 사용자 커스텀 카테고리 생성 UI 추가
- [ ] 카테고리 색상 선택 방식 정의
- [ ] 기본 카테고리 / 사용자 카테고리 구분 표시
- [ ] 카테고리 수정 / 삭제 범위 정의
- [ ] settings 또는 별도 화면에 카테고리 관리 진입점 추가

완료 기준:

- 사용자가 자신의 카테고리를 직접 추가할 수 있다.
- 타이머 / 히스토리 / 리포트에서 새 카테고리를 바로 사용할 수 있다.

### 3. 룰 기반 분석 고도화

- [ ] 중단 사유 입력 구조 검토
- [ ] 방해 요소 메모를 인사이트에 반영할 수 있는지 검토
- [ ] 스트릭 / 완주율 / 편중 규칙 세분화
- [ ] 대시보드용 짧은 인사이트와 주간 리포트용 긴 인사이트를 분리
- [ ] 인사이트 문구 톤 일관성 정리

완료 기준:

- 단순 집계가 아니라 행동 패턴을 더 구체적으로 설명하는 인사이트가 생성된다.
- 대시보드와 주간 리포트의 해석 품질이 현재보다 분명히 좋아진다.

### 4. 성능 정리

- [ ] 탭 이동 시 중복 인증 조회 지점 재확인
- [ ] 쿼리 재사용 가능한 영역 추출 검토
- [ ] 공용 데이터와 사용자별 데이터 분리 검토
- [ ] revalidatePath() 호출 범위 최적화 검토
- [ ] 동적 렌더링 유지 범위 문서화

완료 기준:

- 주요 라우트 이동 시 불필요한 조회가 줄어든다.
- 현재 구조에서 캐시 가능한 영역과 즉시성 유지가 필요한 영역이 정리된다.

### 5. 운영 준비

- [ ] 구글 애널리틱스 도입 범위 결정
- [ ] 페이지뷰 추적 연결
- [ ] 핵심 이벤트 목록 정의
- [ ] 개인정보 최소 수집 기준 정리
- [ ] 배포 환경 변수 정리

완료 기준:

- 기본 페이지뷰와 핵심 전환 이벤트를 수집할 수 있다.
- 이후 제품 사용 흐름 분석을 위한 최소한의 운영 데이터가 쌓인다.

## 추천 진행 순서

1. Settings 확장
2. 카테고리 관리 시작
3. 룰 기반 분석 고도화
4. 성능 정리
5. 운영 준비

## 빠른 실행 단위

이번 작업 사이클에서 바로 시작하기 좋은 단위:

- [ ] settings에 기본 집중/휴식 시간 필드를 먼저 추가
- [ ] `categories` 생성 서버 액션과 입력 폼부터 최소 버전 구현
- [ ] 인사이트 규칙에서 `중단률`, `완주율`, `스트릭` 가중치 재정리
- [ ] 저장/수정 토스트를 타이머와 카테고리 관리 화면에도 재사용할지 결정

## 메모

- 지금은 화면 뼈대보다 “설정값이 실제 동작으로 연결되는지”가 더 중요하다.
- 카테고리 관리는 사용자 체감 가치가 크고, 리포트/히스토리 완성도를 동시에 끌어올릴 수 있다.
- 인사이트 고도화는 새 입력값이 늘수록 품질이 좋아지므로, settings/카테고리 확장과 병행하면 효율이 좋다.
