# DevFlow TODO 리스트

작성일: 2026-04-12

기준 문서:

- `docs/work-log-2026-04-12.md`
- `docs/next-work-roadmap-2026-04-12.md`
- `docs/remaining-work-queue-2026-04-12.md`

## 목적

GTM 배포 이후 남은 Google Analytics 연결 작업과 다음 운영 준비 단위를 정리한다.

## 우선순위 TODO

### 0. 이번에 추가 진행한 기능 작업

- [x] Settings에서 사용자 커스텀 카테고리 생성
- [x] Settings에서 기본 카테고리와 사용자 카테고리 목록 표시
- [x] 새 카테고리 저장 후 타이머, 히스토리, 주간 리포트 재검증 연결
- [x] 사용자 카테고리 이름/색상 수정
- [x] 카테고리 생성/수정 실패 토스트 표시
- [x] 대시보드 오늘 목표까지 남은 집중 시간 표시

다음 확장 후보:

- [ ] 카테고리 삭제 또는 비활성화 정책 정의
- [ ] Settings 기본 집중/휴식 preset 스키마 확장
- [ ] 세션 기록 수정

### 1. GA4 연결 마무리

- [ ] Google Analytics 콘솔 접속 가능 여부 재확인
- [ ] GA4 속성 생성 또는 기존 속성 선택
- [ ] 웹 데이터 스트림 생성 또는 기존 스트림 선택
- [ ] GA4 측정 ID `G-...` 확인
- [ ] GTM에 Google 태그 추가
- [ ] `All Pages` 트리거 연결
- [ ] GTM 변경사항 제출/게시

완료 기준:

- 배포 페이지 접속 시 GA4 실시간 보고서에 page_view가 잡힌다.
- Tag Assistant에서 `GTM-5X65KN66`과 GA4 태그가 모두 정상으로 표시된다.

### 2. 이벤트 추적 기준 정의

- [ ] 이벤트 이름 규칙 정하기
- [ ] 회원가입 시작 이벤트 정의
- [ ] 로그인 성공 이벤트 정의
- [ ] 타이머 시작 이벤트 정의
- [ ] 세션 완료 이벤트 정의
- [ ] 설정 저장 이벤트 정의
- [ ] 주간 리포트 조회 이벤트 정의

완료 기준:

- MVP에서 꼭 볼 행동 지표가 이벤트 목록으로 정리된다.
- 이벤트 이름과 파라미터가 임의로 늘어나지 않도록 기준이 생긴다.

### 3. 운영 설정 정리

- [ ] `NEXT_PUBLIC_SITE_URL`이 Production URL 기준으로 설정되어 있는지 확인
- [ ] Vercel 환경 변수와 로컬 `.env` 역할 분리
- [ ] `.env` 사용 경고 대응 여부 결정
- [ ] Production 배포 후 확인 체크리스트 작성

완료 기준:

- 배포 후 canonical URL, Open Graph URL, sitemap, robots 기준이 Production 주소와 일치한다.
- 로컬 전용 환경 변수와 Vercel 환경 변수가 문서상으로 구분된다.

### 4. 개인정보 최소 수집 기준

- [ ] GA4에서 수집할 이벤트 범위 제한
- [ ] 세션 메모 같은 자유 입력값은 이벤트 파라미터로 보내지 않도록 기준화
- [ ] 사용자 식별자 전송 여부 검토
- [ ] 개인정보 처리방침 업데이트 필요 여부 확인

완료 기준:

- 분석 이벤트가 제품 사용 흐름만 추적하고, 불필요한 사용자 입력값을 수집하지 않는다.

## 추천 진행 순서

1. GA4 측정 ID 확인
2. GTM에서 GA4 태그 게시
3. page_view 수집 확인
4. 핵심 이벤트 목록 확정
5. 개인정보 최소 수집 기준 문서화

## 메모

- Firebase 프로젝트는 현재 웹 GA4 연결에는 필수 아님.
- 이미 사이트에는 GTM 컨테이너가 설치되어 있으므로, 다음 단계는 GTM 내부 설정이다.
- GA4 콘솔 접속 문제가 지속되면 브라우저 확장 프로그램, 로그인 계정 권한, 시크릿 창 접속을 먼저 확인한다.
- 기능 확장과 제품 고도화 후보는 `docs/next-work-roadmap-2026-04-12.md`에서 관리한다.
- 실제 다음 작업 선택은 `docs/remaining-work-queue-2026-04-12.md`를 기준으로 한다.
