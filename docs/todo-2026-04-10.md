# DevFlow TODO 리스트

작성일: 2026-04-10

기준 문서:

- `docs/work-log-2026-04-10.md`

## 목적

2026-04-10 작업 로그를 기준으로, 다음 작업 우선순위와 실행 단위를 체크리스트로 정리한다.

## 우선순위 TODO

### 1. 타이머 상태 고도화

- [ ] 타이머 일시정지 기능 추가
- [ ] 타이머 재개 기능 추가
- [ ] `interrupted` 상태 저장 흐름 구현
- [ ] 중단된 세션의 히스토리 표시 방식 정의
- [ ] active 세션 복원 UX 개선

완료 기준:

- 사용자가 진행 중인 세션을 일시정지하고 다시 이어서 진행할 수 있다.
- 비정상 종료 또는 사용자 중단 상황을 `interrupted` 상태로 저장할 수 있다.
- 새로고침 또는 재방문 시 진행 중 세션이 자연스럽게 복원된다.

### 2. Settings와 목표 연결

- [ ] `daily_goals` 조회 로직 연결
- [ ] 목표 수정 UI 구현
- [ ] 목표 저장 서버 액션 연결
- [ ] 대시보드에 목표 대비 진행률 반영
- [ ] 목표 미설정 상태의 기본 UX 정의

완료 기준:

- 사용자가 일일 목표를 조회하고 수정할 수 있다.
- 대시보드에서 오늘 목표와 실제 집중 시간을 함께 확인할 수 있다.

### 3. 주간 리포트 시작

- [ ] 주간 집계 범위 정의
- [ ] 주간 총 집중 시간 집계 구현
- [ ] 요일별 집계 구현
- [ ] 시간대별 집계 구현
- [ ] 카테고리별 집계 구현
- [ ] 주간 리포트 기본 화면 연결

완료 기준:

- `/reports/weekly`에서 최소 1주 단위 요약 정보를 볼 수 있다.
- 요일별, 시간대별, 카테고리별 패턴이 기본 수준으로 표시된다.

### 4. 성능 정리

- [ ] 탭 이동 시 중복 인증 조회 지점 재점검
- [ ] 공용 데이터와 사용자별 데이터 분리 가능성 검토
- [ ] 불필요한 서버 조회 제거
- [ ] 동적 렌더링 유지 범위 문서화

완료 기준:

- 주요 페이지 전환 시 불필요한 인증/세션 조회가 줄어든다.
- 현재 구조에서 캐시 가능한 영역과 불가능한 영역이 명확히 정리된다.

### 5. 구글 애널리틱스 연결

- [ ] GA 도입 범위 결정
- [ ] 페이지뷰 추적 연결
- [ ] 기본 이벤트 목록 정의
- [ ] 환경 변수 및 배포 설정 정리
- [ ] 개인정보 관점 최소 수집 기준 점검

완료 기준:

- 페이지뷰가 정상 수집된다.
- 이후 이벤트 확장을 위한 기본 추적 구조가 준비된다.

## 추천 진행 순서

1. 타이머 상태 고도화
2. Settings와 목표 연결
3. 주간 리포트 시작
4. 성능 정리
5. 구글 애널리틱스 연결

## 빠른 실행 단위

이번 작업 사이클에서 바로 시작하기 좋은 단위:

- [ ] `sessions` 상태값에 pause/resume 흐름을 어떻게 매핑할지 결정
- [ ] `daily_goals` 조회/수정용 쿼리와 액션 파일 추가
- [ ] `/reports/weekly`에 임시 집계 결과라도 먼저 렌더링
- [ ] 인증 조회 중복이 있는 페이지를 한 번 더 추적 점검

## 메모

- 현재 MVP 범위에서는 타이머 상태 고도화와 목표 연결이 사용자 체감 가치가 가장 크다.
- 주간 리포트는 집계 함수 구조를 먼저 잡고, 시각화는 이후 단계에서 확장해도 된다.
- 성능 최적화는 구조 변경보다 중복 조회 제거 중심으로 접근하는 편이 안전하다.
